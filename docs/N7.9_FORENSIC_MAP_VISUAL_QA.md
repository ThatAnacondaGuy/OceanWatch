# N-7.9 Forensic Map Visual QA Matrix

| Verification Check | Status | Evidence / Notes |
|---|---|---|
| **MAP DOMINANCE** | PASS | Map occupies the central/largest panel in LiveMonitoring and VesselTracking screens. SAR is no longer a blocking floating image; it is a true geographic raster source. |
| **SAR BACKGROUND OPACITY** | PASS | `sarOpacity` defaults to `0.25` in `AppContext`. The texture is visible without overpowering vector overlays. |
| **SLICK VISIBILITY & LABELS** | PASS | Slick is rendered with a solid fill (`0.4`), bright outline (`2.5px`), and a subtle red blur glow (`line-blur: 6`). Added a custom HTML map label explicitly stating "DETECTED OIL SLICK" and "Validation: PASS" floating near the centroid. |
| **DRIFT ORIGIN & ENVELOPE** | PASS | Origin is marked with a clear dashed cyan target. A permanent map label reads "BACKWARD DRIFT ORIGIN". The uncertainty envelope renders as a cyan dashed polygon. |
| **FORWARD FORECAST** | PASS | Uses `forward_forecast.geojson`. When toggled, displays uncertainty ellipses and centerline. MapLibre `symbol` layer dynamically labels nodes with `+1h`, `+2h`, and `+4h`. Added an explicit warning banner stating the projection is anchored to detection time. |
| **VESSEL SYMBOLS** | PASS | Replaced generic diamonds with SVG ship silhouettes. Polygons rotate strictly according to actual AIS `heading`. Colors adhere to forensic palette: Red (Source), White/Blue (Candidate), Amber (Dark). |
| **DYNAMIC TRACKS & PLAYBACK** | PASS | "DEMONSTRATION PLAYBACK" timeline natively drives track geometry. Trajectories are re-calculated on the fly using `playbackData`, stripping future segments. The past path organically reveals itself as the user drags the timeline or presses Play. |
| **SOURCE RELATIONSHIP LINE** | PASS | When the primary source is selected, a distinct cyan dashed line connects the vessel's current playback coordinates directly to the Backward Drift Origin. |
| **COLLAPSIBLE MAP CONTROLS** | PASS | The bulky layer box is replaced by a minimal `☰ LAYERS` button that expands into a compact UI with toggles and native opacity sliders. |
| **FIT CASE BEHAVIOR** | PASS | `Fit Case` button computes a dynamic bounding box including the slick, drift origin, and current vessel positions, ignoring the full extent of the SAR scene. |
| **SATELLITE VIEW TAB** | PASS | Implemented an interactive view in the Dashboard with real toggles for "VV Band", "VH Band" (showing unavailability notice), and "Detection Overlay" (using `mix-blend-screen`). |
| **VESSEL TRACKING LAYOUT** | PASS | Matches 4-pane requirement: Left (Candidates), Center (Map), Right (Intelligence), Bottom (Timeline). Clicking the bar chart in Analytics syncs with the Vessel Tracking selection. |

**FINAL STATUS:** N-7.9 COMPLETE — INVESTIGATION MAP VISUALLY VERIFIED
