# Phase N-7.15.2 QA: Ennore Case Geometry & Synthetic AIS Spatial Alignment

## 1. Coordinate & CRS Audit
- **Slick Centroid**: `[80.348, 13.132]` (Offshore Ennore)
- **Drift Origin**: `[80.412, 13.275]` (Offshore Ennore)
- **MapLibre Coordinate Order**: MapLibre expects `[longitude, latitude]`. Code audit confirmed `DashboardMap.tsx` correctly applies `[pos.lon, pos.lat]`.
- **Root Cause of Map Zooming**: The original `generate_ennore_data.py` script generated synthetic vessel trajectories starting as far away as `[80.590 E, 13.490 N]` (DEMO-MMSI-004), which is roughly 40km from the incident. The frontend's dynamic `fitBounds` correctly included these valid points, which forced the viewport to expand drastically. This extreme zoom-out pulled irrelevant inland geography (like Thervoy Kandigai) into the frame and reduced the actual 106px oil slick polygon to a tiny, visually disconnected speck.

## 2. Synthetic Reconstruction Method
- The trajectories in `generate_ennore_data.py` were surgically recalibrated.
- **Source Vessel (DEMO-MMSI-001)**: Relocated to start at `13.315 N, 80.432 E` and move steadily South-West, ensuring it passes directly through the drift origin `[80.412, 13.275]` at exactly 04:00 UTC (the incident time).
- **Candidate Vessels**: Relocated to physically adjacent offshore positions (e.g., `13.330 N, 80.450 E`) with intersecting or nearby headings.
- All vessels are now constrained within a realistic 10-20km offshore maritime envelope directly surrounding Kamarajar Port. No vessels are generated on land.

## 3. Playback & Attribution Validation
- **Artifact Regeneration**: `ais_tracks.csv`, `ais_tracks.geojson`, and `ais_playback.json` were fully regenerated using the corrected generation script.
- **Attribution Engine Recalculation**: The end-to-end N-5 pipeline (`run_ennore_demo.py`) was executed.
- **Result**: The attribution engine processed the newly aligned spatial metrics, recalculated the 7-factor scores, and successfully confirmed `DEMO-MMSI-001` as the Rank #1 Source with a final probability of **27.6%** (scientifically consistent with the newly generated spatial layout).

## 4. Visual Layout Acceptance
- The frontend dynamically loads the newly tightened GeoJSON bounds.
- The map is now securely zoomed into the Ennore offshore region.
- The slick is large and dominant.
- The vessels (`SOURCE #1`, `DARK VESSEL`, candidate tracks) are intimately clustered around the drift origin and slick.
- Inland India is no longer forcibly rendered.
- Synthetic AIS metadata remains intact.

**Status**: PASSED. Spatial geometry successfully aligned.
