# N-7.10 GIS Map QA Matrix

| Verification Check | Status | Evidence / Notes |
|---|---|---|
| **SCIENTIFIC FIGURE RECTANGLES = 0** | PASS | Wrote `scripts/fix_heatmap.py` to regenerate `drift_heatmap.png` as a pure geographic raster (transparent background, no matplotlib axes, titles, or colorbars). |
| **SLICK = GEOJSON POLYGON** | PASS | Fixed `detected_slick.geojson` using Shapely and Rasterio to correctly project the raw model prediction to `13.132°N, 80.348°E` (true coordinate projection, not a CSS trick). Rendered natively as a MapLibre polygon with red translucent fill and boundary. |
| **DRIFT = TRANSPARENT GEORASTER** | PASS | The backward drift heatmap is applied as a MapLibre raster source anchored geographically. Opacity set to 0.45, preserving basemap visibility. |
| **UNCERTAINTY = GEOJSON** | PASS | Drift uncertainty envelope (`drift_uncertainty.geojson`) renders correctly as a geographic feature (`#06b6d4` polygon). |
| **FORECAST = GEOJSON** | PASS | Forward forecast renders perfectly with MapLibre line and fill geometries. Forecast nodes (+1h, +2h, +4h) are native symbol layers. |
| **VESSELS = ACTUAL TRACKS + BOAT MARKERS** | PASS | Replaced diamond markers with custom inline SVG ship silhouettes that rotate dynamically with `v.heading`. Trajectories render as correct geo-lines: thick red (Source), dashed amber (Dark), thin neutral (Candidate). |
| **SAR = GEOREFERENCED RASTER** | PASS | SAR is loaded as a MapLibre image source overlaid on the OSM tiles, defaulting to 25% opacity so it acts as an evidence background, not a giant blocking rectangle. |
| **TIMELINE = MOVES VESSELS** | PASS | Timeline interactions directly update the geographic `LngLat` coordinates of vessel markers and mathematically truncate trajectory lines based on `playbackTime` via `ais_playback.json`. |
| **SOURCE SELECTION = MAP SYNCHRONIZED** | PASS | Clicking a candidate highlights its track, brings the vessel to the highest z-index, flies the map view to its location, and dynamically renders the `Source -> Origin` connection line if the primary source is chosen. Vessel labels decluttered (show only on hover/selection). |
| **MAP INTERACTIVE = PASS** | PASS | All layers (SAR, Model, Slick, Drift, Forecast, Tracks) are fully interactive and controllable via a compact top-left `☰ LAYERS` dropdown. |

**FINAL STATUS:** N-7.10 COMPLETE — TRUE GIS FORENSIC MAP
