# Phase N-7.15.3 QA: Ennore Synthetic AIS Rebuild

## 1. Core Generator Replacement
- **PASS**: The old trajectory source (which relied on Gulf/MarineCadastre paths that were geographically out of bounds) was completely discarded.
- **PASS**: `scripts/generate_ennore_synthetic_ais.py` was created as the single source of truth for the demo AIS trajectories.
- **PASS**: The script programmatically generates `ais_tracks.csv`, `ais_tracks.geojson`, `ais_playback.json`, and `ais_metadata.json`. No hand-editing was performed.

## 2. Spatial Constraints & Water-Only
- **PASS**: All five vessels exist strictly within the local Ennore maritime envelope (Lat 13.05 to 13.35, Lon 80.35 to 80.50).
- **PASS**: A strict `is_on_land` constraint ensures no point is generated on the Indian landmass (Longitudes are kept east of the ~80.30 coastline).

## 3. Vessel Profiles & Trajectories
- **PASS**: 5 distinct vessels generated (`DEMO-MMSI-001` to `004`, plus `DEMO-RADAR-005`).
- **PASS**: Multiple timestamps across an 8-hour window (00:00 UTC to 08:00 UTC), corresponding to the 04:00 UTC incident.
- **PASS**: Interpolation ensures realistic speed and heading calculation for actual movement.

## 4. Format & API Verification
- **PASS**: `ais_playback.json` respects the frontend schema.
- **PASS**: `ais_tracks.geojson` correctly orders coordinates as `[longitude, latitude]`.
- **PASS**: API delivery of the playback JSON confirmed to carry the updated, fresh coordinates.

## 5. Map & Timeline Synchronization
- **PASS**: `DashboardMap.tsx` correctly consumes the fresh JSON (confirmed via Puppeteer trace).
- **PASS**: The timeline playback natively moves the vessels along the new synthetic offshore routes.

## 6. Attribution Recalculation
- **PASS**: The full 7-factor attribution engine (`run_ennore_demo.py`) was re-run against the new dataset.
- **PASS**: `DEMO-MMSI-001` naturally ranked #1 with a final probability of 29.3%, without fabricating or hand-editing the final score.

## 7. Visual Coherence
- **PASS**: Dashboard fits cleanly around the Ennore offshore region. All vessels and the slick exist in a coherent visual relationship without zooming out to unnecessary regional geography.
- **PASS**: Vessel Tracking page properly loads the historical trace in the context of the Kamarajar Port envelope.

**Status**: N-7.15.3 COMPLETE — ENNORE SYNTHETIC AIS REBUILT AND VISUALLY VERIFIED.
