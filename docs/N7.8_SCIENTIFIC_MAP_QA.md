# N-7.8 Scientific Map QA Matrix

| Verification Check | Status | Evidence / Notes |
|---|---|---|
| **RAW U-NET OUTPUT** | PASS | Re-ran `models/best_full_oil_unet.pth` inference in `generate_ennore_drift_and_unet.py`. Persisted float32 `.tif` and web-compatible `.png` probabilities. Exposed via API. |
| **SLICK FOOTPRINT** | PASS | Rendered GeoJSON polygon with outline (`#ef4444`) and centroid marker. Clickable popup displays Area, Validation Status, GLCM, and Model info. |
| **DRIFT HEATMAP** | PASS | Backward Lagrangian drift heatmap rendered as georeferenced map raster layer (`raster-opacity: 0.55`). |
| **DRIFT ORIGIN** | PASS | Distinct dashed blue marker placed at origin coords. Clickable popup shows time window and uncertainty params. |
| **DRIFT UNCERTAINTY** | PASS | Procedurally generated elliptical envelope based on 8hr diffusion physics (D=10). Rendered as dashed polygon. |
| **FORWARD FORECAST** | PASS | Generated physically consistent forecast (`U_oil = U_current + 0.03 * U_wind`) representing +1h, +2h, +4h uncertainty polygons with a connecting centerline. |
| **VESSEL TRACKS** | PASS | Rendered `ais_tracks.geojson` as LineStrings. Source is solid red (width 3), dark vessels are dashed amber, candidates are dashed neutral. |
| **VESSEL SELECTION SYNC** | PASS | Bidirectional React state: Clicking map markers highlights track & table row. Clicking table flies map to vessel coordinates. |
| **TIMELINE PLAYBACK** | PASS | "Demonstration Playback" timeline uses actual `ais_playback.json` epoch timestamps to dynamically interpolate vessel marker `[lon, lat]` positions via React effects. |
| **MAP LAYERS** | PASS | Floating collapsible layer panel implemented with independent toggles for SAR, Model Detection, Slick, Drift, Forecast, and Tracks. Includes SAR/U-Net opacity sliders. |
| **NO STATIC FORENSIC ELEMENTS** | PASS | The central map is entirely interactive MapLibre GL. SAR is a raster source, not a floating HTML image. Dashboard tabs (Live Map, Satellite, History) display dynamic contexts. |
