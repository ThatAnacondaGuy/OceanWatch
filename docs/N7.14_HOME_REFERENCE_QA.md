# Phase N-7.14 - Home Page Reference QA

## Layout & Proportions
- [x] Header MATCH (Gov identity, brand, tagline, user, notifications, date context all visually aligned).
- [x] Sidebar MATCH (Dark navy layout, icons, help/logout regions matching exactly).
- [x] KPI Row MATCH (Four major metric cards + date/status card matching reference structure but populated with true 2017 Ennore dataset).
- [x] Map Size MATCH (Map scales properly at 1440x900 and 1672x941 without cropping).
- [x] Map Composition MATCH (Dynamic `fitBounds` ensures true offshore framing).
- [x] Selected Incident MATCH (Incident ID, location, source, attribution score, evidence, and status correctly formatted and styled).
- [x] Recent Detections MATCH (Shows ENNORE-2017 + truthful "Additional incidents unavailable" text, matching reference layout).
- [x] Bottom Row MATCH (Wind/Ocean, System Notifications (truthful events), Quick Actions functionally mapped).
- [x] Footer MATCH (Includes "Demonstration Release" / "Government-style demonstration interface").

## Design System
- [x] Typography MATCH (Uses inter/sans-serif with identical weight and size progression).
- [x] Colors MATCH (Navy sidebars, slate backgrounds, standard UI accents).
- [x] Spacing MATCH (Flex gaps and padding replicate the desktop UI mockup spacing).

## Functional Integrity
- [x] Map Functionality PASS (Interactivity remains fully supported).
- [x] Data Wiring PASS (All UI strings bound to `useApp()` / real backend endpoints).
- [x] No Fake Metrics PASS (Example metrics (e.g. 1,248 vessels) correctly replaced with our actual N-7 pipeline data).
- [x] No False Official Claim PASS ("For Official Use Only" substituted with explicit Demonstration text).
