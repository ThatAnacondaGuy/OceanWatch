# Phase N-7.15 - Dashboard Map Rollback QA

## Map Aesthetics & Configuration
- [x] Dark Base Map = PASS (Restored extreme desaturated OpenStreetMap logic `raster-saturation: -1`, `raster-brightness-max: 0.2` creating a charcoal/forensic dark intelligence appearance).
- [x] NO SATELLITE-STYLE DASHBOARD MAP = PASS (All Esri World Imagery removed from `DashboardMap.tsx`, including the mini-map which is now an inverted dark OSM tile).
- [x] Compact Forensic Controls = PASS (Map overlay dropdowns and Legend were rolled back to strict dark theme: `bg-slate-900/95 text-slate-200 border-slate-700`).

## Functional Data Integrity
- [x] Ennore Case = PASS (Dashboard retains `ENNORE-2017-DEMO` case geometry seamlessly).
- [x] SAR = PASS (Original SAR raster loads natively over the dark ocean).
- [x] Slick = PASS (Actual AI detected footprint renders explicitly over the dark map).
- [x] Drift = PASS (Ensemble heatmap remains accurately overlayed).
- [x] Origin = PASS (Backward Drift Origin rendered securely).
- [x] Uncertainty = PASS (Cyan uncertainty bounding envelope rendered correctly).
- [x] Forecast = PASS (Forward forecast geometries remain functionally toggleable).
- [x] Vessels = PASS (Functional AIS kinematics restored over dark basemap).
- [x] Tracks = PASS (AIS historical trailing tracks cleanly rendered).
- [x] Timeline = PASS (Case playback timeline remains active and connected).

## External Dashboards
- [x] Case Panel = PASS (Selected Incident panel explicitly confirmed as untouched, retaining truthful Synthetic metrics).
